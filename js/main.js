// main.js - Entry point for the terminal portfolio
import { TerminalPortfolio } from './terminal.js';

// Initialize the terminal when the page loads
document.addEventListener('DOMContentLoaded', () => {
    new TerminalPortfolio();
});